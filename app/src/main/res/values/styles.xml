<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@null</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:itemBackground">@color/black</item>
        <item name="android:textColor">@color/font</item>
<!--        <item name="android:background">@color/black</item>-->

    </style>
    <style name="SatelliteMenu" parent="Widget.AppCompat.PopupMenu">
        <item name="android:textColor">#39FF14</item>
<!--        <item name="android:itemBackground">@drawable/ic_moneybar</item>-->
    </style>
    <style name="SplashScreen" parent="@android:style/Theme.DeviceDefault.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_screen</item>
    </style>

</resources>
